# What the laptop's workers are RUNNING. Not what a tree is checked out at.
#
# The compute node's timer reads this with
#     git show origin/main:plans/DECLARED-REVISION.txt
# every few minutes, and syncs itself to it. It refuses rather than guesses:
# a sha absent from the clone, a sha that is not an ancestor of origin/develop,
# a dirty deploy slot, or an install that leaves a sibling wrong all leave the
# worker DOWN. A silent wrong consumer is worse than no consumer.
#
# WHY "RUNNING" AND NOT "CHECKED OUT". On 2026-08-30 a branch was checked out
# in this machine's deploy tree while a twelve arm sweep was live. The workers
# had loaded their code hours earlier and never reloaded, so every arm computed
# identically, but two of them recorded a revision no process had ever held,
# and six batches of a third were refused and dead-lettered by the guard that
# noticed. The tree and the running code are two different quantities. This
# file names the second.
#
# Every sibling package is derived from this commit's own poetry.lock, so there
# is no second list to disagree with. Add an override line only when knowingly
# ahead of the lock, one per package, first match wins.
#
# WHEN NOT TO MOVE IT. Not while a campaign is live. The node's guard defers
# while a batch is in flight and then pins to the new sha, so it will REFUSE
# the remaining batches of the campaign it was already serving. That is the
# guard working: no prediction set ends up holding rows written by two code
# versions, which is the failure the 2026-08-30 sweep produced. But the refusal
# is invisible from the server, where it looks like a queue with a consumer
# that is not consuming, and a failure that is safe and invisible is the worst
# kind to diagnose. It already cost an afternoon once, on the sibling pin.
#
# So the check before editing this file is on the SERVER and not on the node:
#     no job in QUEUED or RUNNING, and no prediction set being written.
# The node's guard covers its own half of the risk. It cannot see the queue.
#
# Update this when the WORKERS move, not when the tree does.

# 2026-09-01: bc7c4237 -> ea40bf90 -> 853322e8 -> f17c4819 -> a8215b38 ->
# a5de702, six shas in one sitting. The last is the one to read; the earlier
# ones are left described because the node polled between them and its state
# file will name ea40bf90. It is powered off, so all of them collapse into one
# sync, which is why a run of hops this long cost nothing here and would not
# have been acceptable with the fleet up.
#
# THE DECLARATION TRACKS THE TIP OF develop. The third hop changes one test
# docstring and nothing a worker executes, and it moved the declaration anyway.
# That is the rule now, replacing a judgment call. Deciding per commit whether a
# change is "real enough" to declare is how the file drifts behind: every skip
# is defensible on its own and the gap is only visible once something depends on
# it. A hop that changes nothing costs the node one stop and restart with no
# work in flight, which is cheaper than the gap.
#
# The server check the section above demands was run before each, and is
# recorded in the commits: no job in QUEUED or RUNNING, no work queue holding
# messages, none unacknowledged, newest prediction_set 2026-08-30 08:07Z. The
# only thing in the broker is the six dead-lettered batches from 2026-08-30,
# which are not in flight and are not waiting on a consumer.
#
# MIGRATIONS. One, e2f7c3a1b904 (a result records the depth it scored), added
# between bc7c4237 and ea40bf90 and unchanged since. The server's
# alembic_version is already at it. That is what the schema-applied line below
# asserts and the only thing it asserts. A node still sitting at bc7c4237 sees
# the migration in its diff and needs the line; one that already followed to
# ea40bf90 sees no migration in the second hop and ignores it.
#
# SIBLINGS. cafaeval-protea moves 2cb2e66d -> e937e0e with this. That is the
# point of the second hop, not a side effect: the two kernels differ in whether
# the pooled sums are restricted to the eligible rows, so a fleet split across
# them would produce PK numbers labelled comparable that are not. PROTEA#917.
# The server's own venv was reinstalled to e937e0e before this was written, so
# the machine making the declaration satisfies it.

# 2026-09-02: a5de702 -> c4b1204, and the check that gates this file was run
# on the server as the header requires: no job in QUEUED or RUNNING and no
# prediction set being written at the moment of the edit.
#
# WHY NOW. Overnight this machine ran three revisions on one queue at once. Its
# worker processes had loaded their code on 30 August and never reloaded, so
# they opened prediction sets stamping b0f164dc; the compute node was in-sync
# at a5de702 and correctly refused every batch it was handed; and the deploy
# tree was further on than either. Two arms of a declared experiment failed
# with ForeignRevisionError and a third was cancelled before it finished a
# short prediction set. Nothing was mislabelled, because the node refuses
# rather than guesses.
#
# The workers were then restarted against a5de702, which is why the machine
# making this declaration is entitled to make it. Moving to c4b1204 now carries
# the fixes that came out of the same night: the depth field no longer renders
# three quantities under one axis name (PROTEA#925), the paired comparison's
# operating point is a declared field rather than a literal (#926), the frame
# audit no longer subtracts two populations (#924), and the server gained the
# check the node already had, refusing to start a worker on a revision nobody
# declared (#928).
#
# NO MIGRATION MOVES. `git diff --name-only a5de702 c4b1204 -- alembic/versions/`
# is empty, so schema-applied simply follows the coordinator and no node needs
# to consult it on this hop.
#
# SIBLINGS unchanged: cafaeval-protea stays at e937e0e, which a5de702's own
# poetry.lock already named.

# 2026-09-02, second hop of the day: c4b1204 -> 8f43cea. Server gate run before
# the edit as the header requires: no job QUEUED or RUNNING, no prediction set
# being written. No migration moves, so schema-applied follows the coordinator.
#
# WHAT IT CARRIES. PROTEA#929, the revision guard stops guessing: neither the
# deploy tree nor the farm root has a default any more, it compares DEPLOY
# against its own working directory so it cannot watch a tree the worker is not
# starting in, and a missing farm clone is a refusal rather than an allowance.
# PROTEA#930, the reason this hop is worth making today: build_graph handed the
# declared floors to one node builder out of ten, so nine nodes could not reach
# `measured` whatever an experiment_run declared. PROTEA#922, the split registry
# had the TUNE window inside the validation split. PROTEA#923, the seal named
# the harness label instead of the digest column it writes.

# 2026-09-02, third hop: 8f43cea -> 957e975. Server gate run before the edit:
# no job QUEUED or RUNNING, no prediction set being written. No migration moves.
#
# WHAT IT CARRIES, and it is the last of the stabilisation. PROTEA#931: a
# separation that crosses frames is not one. separated_from_floor tested a
# declared floor against every level that landed in the same panel, which is
# every arm the record holds whatever window it was scored under; a panel key is
# a category and an aspect and it is not a frame. It now restricts the
# comparison to the rows sharing the floor's seal, refuses an unsealed floor
# instead of reporting that it did not separate, and refuses a floor whose rows
# span two seals instead of picking one.
#
# The same PR declares frame_digest on the EvaluationResult model. It had zero
# references in protea/infrastructure and was reachable only through raw SQL in
# two files, so it was invisible to Base.metadata: a query reading it passed
# against the live store and failed against a schema built from the models. That
# invisibility is also how the seal came to describe the wrong column for a day.

# 2026-09-02, fourth hop: 957e975 -> 9af9204. THIS ONE MOVES A MIGRATION, so the
# schema-applied line below is load-bearing and not ceremony: a node that follows
# the coordinator without it runs code that sets three new NOT NULL columns
# against a table that has none, and every insert fails from the node's side with
# nothing on the node able to explain why. The schema lives on this machine.
#
# THE SERVER GATE the header demands was run before this edit and it has never
# been this clean: no job in QUEUED or RUNNING, zero prediction sets in the
# database, no queue in the broker holding a message. The campaign was reset
# earlier today, so there is nothing in flight for the node's guard to refuse.
# This is the cheapest moment a migration hop will ever get.
#
# WHAT IT CARRIES. PROTEA#933 only. An EvaluationSet was UNIQUE on (old, new),
# which treats a pair of GOA releases as if it settled which delta the row holds.
# It does not. Measured on 220 -> 227 with experimental ground truth: propagating
# each side under its own native DAG instead of both under the pivot moves the PK
# bucket by 21 percent of its annotations and by 51 percent of its proteins, and
# NK and LK move by exactly zero, which is the check that the count is right. The
# cause is 592 terms whose ancestors were rewired between the two releases; the
# top twelve are half of it. Nobody annotated anything.
#
# So the three snapshots that decide the delta became columns and joined the key,
# stored resolved rather than as the overrides that were requested.
#
# THE MIGRATION, d4b8c2f10a37, is already applied here. It backfilled the one row
# this database holds, b7cfed9a, from the producing job's payload rather than
# from the annotation sets: GOA 220 is bound to an ontology eleven months after
# its own publication, so the set's own binding would have recorded a graph that
# never ran. The row now reads pivot 2024-03-28, natives 2024-03-28 and
# 2025-07-22, which is exactly what was asked for.
#
# WHAT WIDENING THE KEY REOPENED, and why it is worth knowing on the node too:
# _resolve_train_split_eval called .one_or_none() on the bare pair, and that call
# raising MultipleResultsFound is how the LB.1 incident of 2026-05-12 read from
# outside. It now collects candidates and, when there is more than one, names
# them with the graphs that tell them apart and stops.
#
# NO SIBLING MOVES. cafaeval-protea stays at e937e0e.
#
# WHAT IT COST TO RESTART, because it is the header's own subject. An idle
# worker is the SLOWEST one to stop: the consumer only notices the stop signal
# when it wakes to handle a message, so protea.predictions.batch sat
# "deactivating" for its full five-minute TimeoutStopSec and was killed at the
# deadline. Ten units at five minutes of worst case do not fit in one sequential
# pass, and the first attempt here was cut off after restarting five of them.
#
# That left five workers still running 957e975 while five ran 9af9204 -- which is
# the exact shape of the 30 August incident this file was written about, arrived
# at from the other direction, and it was two minutes from being declared as
# though all ten had moved. The check that caught it was reading
# ExecMainStartTimestamp on every unit rather than trusting that "restart"
# returned. Nothing was in flight, so nothing was mislabelled.
#
# The rule that follows: restart the units CONCURRENTLY, and verify by timestamp
# per unit before editing this file. "systemctl restart" returning is not
# evidence, and neither is is-active -- a unit that never stopped is also active.

# 2026-09-02, fifth hop: 9af9204 -> fb9f766. NO MIGRATION MOVES, so
# schema-applied simply follows the coordinator and no node needs to consult it.
# Server gate run before the edit: no job QUEUED or RUNNING, zero prediction sets.
#
# WHAT IT CARRIES. PROTEA#934, and it is the defect the previous hop uncovered by
# making the case reachable. generate_evaluation_set chose between its two delta
# paths on whether the three snapshot ARGUMENTS coincided. The fast path resolves
# annotations through go_term.id, a column scoped to a snapshot, so it is correct
# only when the annotation SETS are bound to the graph in use. The two tests agree
# until an override is passed and diverge exactly when one is. Of the 11,197,453
# experimental annotations in the 220 -> 227 corpora, ZERO resolve under the pivot
# they were asked to be read in; every one was dropped without comment and a
# well-formed EvaluationSet was stored holding zero of everything. It did not
# raise, and nothing downstream could have told it from a real one.
#
# The mode now leads on the sets' own bindings, and an empty delta over non-empty
# corpora is refused rather than persisted.
#
# THE RESTART, under the rule the previous entry wrote. All ten units concurrently,
# verified by ExecMainStartTimestamp one by one: 21:46:37 to 21:51:37. Three took
# the full five-minute TimeoutStopSec, the idle ones, exactly as predicted. In
# parallel that is five minutes total rather than twenty-five, and no window in
# which half the fleet ran one revision and half another.
#
# fb9f766 -> 8699bfd. Two commits, and a schema change that made the move
# obligatory rather than optional.
#
# 46ceaf1 gives the GOA loader the check it never had. Every GAF declares the GO
# build it was generated against in a !go-version header, and nothing was reading
# it: ontology_snapshot_id was a required payload field with no default and no
# validation, so GOA 220 was loaded bound to an ontology eleven months after its
# own publication and nothing objected. Evaluation survived it, because the
# reconcile works in go_id text space; prediction could not, because a predicted
# term IS a donor annotation's go_term_id. The loader now refuses a snapshot
# newer than the declared build, and refuses too when the header cannot be read
# unless the payload says allow_unverified_ontology -- a guard that passes
# silently when it cannot check is not a guard.
#
# 8699bfd renames the embedding registry. It also ADDS embedding_config.kind as
# NOT NULL with no server default, which is why this move could not wait: the
# migration is already applied to the live store, so code that predates the
# column cannot insert an embedding_config at all. A server default was rejected
# -- defaulting to 'pretrained' would silently misclassify a learned config,
# which is the invisible-failure shape this file exists to prevent.
#
# THE WINDOW. Moved with no campaign live: nothing queued, and the only
# experiment_run marked abandoned. That is the condition the entry above asks
# for, and it will not hold again once phase 0 starts.
#
# THE RESTART. All ten units concurrently, verified per unit by
# ExecMainStartTimestamp: 21:31:34 to 21:36:34. Three took the full five-minute
# TimeoutStopSec, the idle ones -- the same three as last time
# (embeddings.write, predictions.batch, predictions.write). The worker log now
# also prints its own sha at startup, which is stronger evidence than a
# timestamp: "Worker started. queue=protea.jobs revision=8699bfd7...".
#
# 8699bfd -> 5674c93. Dos entradas, y la segunda desbloquea un eje.
#
# fc6fe78 lleva los tres apagones silenciosos: el broker OOM con su watermark
# 26 veces por encima de su techo, la cache de referencias cuya frescura de 300 s
# era mas corta que la consulta de 338 s que existe para evitar, y el latido que
# ahora vigila colas retenidas sin consumidor. Ninguno era defecto nuevo: la
# campana es lo primero que apreto lo suficiente para encontrarlos.
#
# 5674c93 arregla dos cosas de la misma forma: algo identificado por un nombre
# que se mantuvo estable mientras cambiaba lo que el nombre significaba.
#
#   La clave de la cache de pool nombra el PUNTERO, no el contenido. GOA 220 se
#   reparo el 04-09 de releases/2025-03-16 a releases/2024-03-28, y los ids de
#   termino GO son por snapshot. El id del conjunto no cambio; cambio lo que ese
#   id significa. 517 ficheros aqui y 260 en el nodo quedaron con anotaciones de
#   donante resueltas contra una ontologia que el conjunto ya no declara, bajo
#   una clave todavia valida: 0 de 4.000 ids muestreados pertenecen al snapshot
#   actual, y los rangos ni se solapan. Nada lo habria cazado. _CACHE_EPOCH va a
#   3, que es el instrumento que el propio docstring del fichero defiende porque
#   no exige juzgar que entradas estan afectadas.
#
#   VERIFICADO ANTES DE DESPLEGAR: los ejes A, B y C no leyeron nada envenenado.
#   Cero ficheros de politica permisiva son anteriores a la reparacion, y la
#   campana limpia corre permisiva. Fue suerte, no diseno.
#
#   Y ref_data_by_aspect era un nombre sobre dos estructuras, lo que bloqueaba
#   entero el brazo de separacion por aspecto del eje C.
#
# LA VENTANA. Movida con cero trabajos en vuelo, verificado en la base antes de
# tocar nada. Es la condicion que la cabecera de arriba pide.
#
# EL REINICIO. Once unidades concurrentes, verificadas una a una por
# ExecMainStartTimestamp Y por la revision que cada worker autoinforma al
# arrancar. Diez movieron a la primera; predictions.write fue RECHAZADA por su
# propio ExecStartPre porque un escritor TRANSITORIO lanzado el 06-09 para
# drenar una cola seguia vivo -- con la revision anterior, y siendo el unico
# consumidor de esa cola. El guardia hizo exactamente su trabajo. Parado el
# transitorio, la unidad arranco y autoinforma 5674c933 como las demas.

# 2026-09-15: 087fefeb -> d94684e, el primer movimiento tras el borrado de la
# maquina. Chequeo del servidor corrido justo antes de esta edicion, como exige
# la cabecera: 0 jobs en QUEUED o RUNNING, 0 prediction sets, ninguna cola del
# broker reteniendo mensajes. Ninguna migracion se mueve entre 5674c933 y
# d94684e, asi que schema-applied se queda y ningun nodo necesita consultarlo.
#
# POR QUE AHORA, y no es una mejora: sin esta linea no hay worker. El guardia de
# revision rechaza arrancar sobre un arbol que la declaracion no nombra, y con el
# worker abajo TODA operacion acaba corriendose fuera de la plataforma. Eso es
# exactamente lo que acaba de pasar: un insert_proteins conducido a mano metio
# 91.000 proteinas con la tabla job en cero y GET /v1/jobs devolviendo []. Las
# filas estaban y el trabajo no existia -- sin procedencia, sin eventos, sin nada
# que cancelar, y sin nada en pantalla. La declaracion de 087fefeb es del 07-09 y
# es anterior al borrado, asi que llevaba caida desde que la maquina volvio.
#
# QUE LLEVA. Dieciseis commits que ya estaban en origin/develop y que la
# declaracion no habia seguido, del 913f5ea al d94684e. La punta de develop, que
# es la regla que esta cabecera fijo para no decidir commit a commit si un cambio
# es "suficientemente real".
#
# QUE NO LLEVA, a proposito. El pulido de dependencias y la guarda de
# tokenizacion dorada escritos hoy NO entran aqui: van en PROTEA#962 y esta
# declaracion no los nombra. Se hizo asi para que el sha declarado sea uno que ya
# esta en origin y que el nodo puede encontrar en su clon. Declarar un commit
# local lo habria dejado ABAJO por ausente -- el fallo seguro, y tambien el
# invisible desde el servidor.
#
# UNA DIVERGENCIA CONOCIDA mientras #962 no entre: el venv de este servidor tiene
# python-multipart 0.0.32 y el poetry.lock de d94684e nombra 0.0.22. Es
# deliberado y no se revierte: esa version acumula doce avisos, dos HIGH, los dos
# de denegacion de servicio en la ruta de subida, y la API esta publicada por
# ngrok. Se resuelve sola al entrar #962.

# 2026-09-15, segundo salto: d94684e -> 0de335d. Puerta del servidor corrida
# justo antes de la edicion: 0 jobs en QUEUED o RUNNING, 0 prediction sets,
# ninguna cola del broker reteniendo mensajes. Ninguna migracion se mueve.
#
# POR QUE AHORA. Stage A paso 1 acaba de terminar -- insert_proteins SUCCEEDED,
# 617.103 proteinas, 528.545 secuencias distintas -- y lo que viene son 71
# releases de GOA. Ese es el momento de mover, no despues: el salto lleva
# PROTEA#963, que estampa las versiones de libreria de cada job en la maquina
# que lo ejecuta, y sin el las 71 cargas quedarian sin sellar. El propio
# insert_proteins que acaba de correr lo demuestra: cero eventos provenance.*,
# porque el worker arranco antes de fusionar.
#
# QUE LLEVA. PROTEA#962 (la pila que un arranque en frio levanta -- pg18 con su
# montaje nuevo, minio desde quay.io, los ocho servicios por digest, y el fixture
# de integracion movido de pg16 a pg18; el pulido de dependencias, con
# python-multipart fuera de una version con doce avisos; y la guarda de
# tokenizacion dorada) y PROTEA#963 (la procedencia de librerias).
#
# AMBOS ESTAN EN origin/develop, a diferencia del salto anterior: 0de335d es un
# sha que el nodo puede encontrar en su clon.

# 2026-09-23, tercer salto: 0de335d -> 9b6e9e4. Puerta del servidor corrida
# justo antes de la edicion: 0 jobs en QUEUED o RUNNING, 0 prediction sets,
# protea.jobs con 0 mensajes y sin consumidor. Ninguna migracion se mueve, y ni
# pyproject ni poetry.lock cambian: el salto es codigo puro.
#
# POR QUE AHORA. En 24 horas el reaper cancelo 21 cargas de GOA que no tenian
# nada de malo. Un job encolado y todavia sin consumir le parecia huerfano, asi
# que lo cancelaba; el patron se repitio tres noches seguidas y cada vez costo
# volver a encolar a mano. Quedan 13 releases (218-230) y sin este salto se
# cancelan igual. El arreglo no es pelearse con el proceso -- deshabilitar la
# unidad de systemd no basta, porque scripts/manage.sh arranca su propio reaper
# con ruta relativa y eso systemd no lo cubre -- sino que el reaper deje de
# estar roto.
#
# QUE LLEVA.
#   PROTEA#964: el sello de procedencia estaba en OperationConsumer, que sirve
#     colas por lotes sin fila de job. Pasa a BaseWorker, que es donde hay un
#     job que sellar. Las 53 releases ya cargadas no lo llevan.
#   PROTEA#965: fetch_header devolvia "" cuando no podia leer la cabecera del
#     GAF, y "no he podido preguntar" quedaba indistinguible de "no declara
#     version". Ahora reintenta y levanta HeaderUnreadableError, que el load
#     convierte en RetryLaterError.
#   PROTEA#966: un reintento creaba un annotation_set nuevo en lugar de reusar
#     el que ya existia para (source, source_version, ontology_snapshot_id).
#     La release 184 tiene hoy dos sets por esto, y es la unica.
#   PROTEA#967: el reaper pregunta al broker cuantos mensajes quedan en la cola
#     antes de llamar huerfano a un job en QUEUED. Si el broker no contesta, no
#     cancela.
#
# Los cuatro estan en origin/develop, y 9b6e9e4 es un sha que el nodo encuentra
# en su clon.

coordinator 9b6e9e41c8cc9fc2780527ef088530043fec9f06
schema-applied 5674c9331ce079a8abd275c9078b49befbfa2670
