---
description: List available agent types
---

Run `ls ~/Thesis2/agent-farm/agents/` to display all registered agent types. Each `.yaml` file is a runnable agent definition (skip `_template.yaml`). Report the agent menu to the user.
